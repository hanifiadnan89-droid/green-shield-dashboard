{
  "name": "green-shield-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c cyan,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "render:build": "npm run install:all && cd server && npx playwright install chromium && cd .. && npm run build",
    "start": "npm start --prefix server",
    "fieldroutes:export-auth": "node scripts/fieldRoutesExportAuth.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
